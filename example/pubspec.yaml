name: atomic_webview_example
description: Demonstrates the supported atomic_webview API.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  atomic_webview:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/example.html
